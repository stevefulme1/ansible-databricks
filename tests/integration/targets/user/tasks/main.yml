---
# Integration tests for stevefulme1.databricks.user

- name: Create user
  stevefulme1.databricks.user:
    user_name: "ansible-test@example.com"
    display_name: "Ansible Test"
    state: present
    host: "{{ databricks_host }}"
    token: "{{ databricks_token }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: create_result

- name: Verify creation
  ansible.builtin.assert:
    that:
      - create_result is changed

- name: Create user again (idempotency)
  stevefulme1.databricks.user:
    user_name: "ansible-test@example.com"
    display_name: "Ansible Test"
    state: present
    host: "{{ databricks_host }}"
    token: "{{ databricks_token }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: idem_result

- name: Verify idempotency
  ansible.builtin.assert:
    that:
      - idem_result is not changed

- name: Update user
  stevefulme1.databricks.user:
    user_name: "ansible-test@example.com"
    display_name: "Ansible Test Updated"
    state: present
    host: "{{ databricks_host }}"
    token: "{{ databricks_token }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: update_result

- name: Verify update
  ansible.builtin.assert:
    that:
      - update_result is changed

- name: Delete user
  stevefulme1.databricks.user:
    user_name: "ansible-test@example.com"
    state: absent
    host: "{{ databricks_host }}"
    token: "{{ databricks_token }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: delete_result

- name: Verify deletion
  ansible.builtin.assert:
    that:
      - delete_result is changed

- name: Delete user again (idempotency)
  stevefulme1.databricks.user:
    user_name: "ansible-test@example.com"
    state: absent
    host: "{{ databricks_host }}"
    token: "{{ databricks_token }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: delete_idem

- name: Verify delete idempotency
  ansible.builtin.assert:
    that:
      - delete_idem is not changed
