---
# Integration tests for stevefulme1.databricks.job
# These tests require valid credentials configured in integration_config.yml

- name: Create job (check_mode)
  stevefulme1.databricks.job:
    host: "{{ databricks_host }}"
    token: "{{ databricks_token }}"
    state: present
    name: "ansible-test-{{ resource_prefix }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  check_mode: true
  register: check_result

- name: Verify check_mode reports changed
  ansible.builtin.assert:
    that:
      - check_result is changed

- name: Create job
  stevefulme1.databricks.job:
    host: "{{ databricks_host }}"
    token: "{{ databricks_token }}"
    state: present
    name: "ansible-test-{{ resource_prefix }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: create_result

- name: Verify job was created
  ansible.builtin.assert:
    that:
      - create_result is changed
      - create_result.job is defined

- name: Create job again (idempotency)
  stevefulme1.databricks.job:
    host: "{{ databricks_host }}"
    token: "{{ databricks_token }}"
    state: present
    name: "ansible-test-{{ resource_prefix }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: idem_result

- name: Verify idempotent create
  ansible.builtin.assert:
    that:
      - idem_result is not changed

- name: Delete job (check_mode)
  stevefulme1.databricks.job:
    host: "{{ databricks_host }}"
    token: "{{ databricks_token }}"
    state: absent
    name: "ansible-test-{{ resource_prefix }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  check_mode: true
  register: delete_check

- name: Verify check_mode delete reports changed
  ansible.builtin.assert:
    that:
      - delete_check is changed

- name: Delete job
  stevefulme1.databricks.job:
    host: "{{ databricks_host }}"
    token: "{{ databricks_token }}"
    state: absent
    name: "ansible-test-{{ resource_prefix }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: delete_result

- name: Verify job was deleted
  ansible.builtin.assert:
    that:
      - delete_result is changed

- name: Delete job again (idempotent)
  stevefulme1.databricks.job:
    host: "{{ databricks_host }}"
    token: "{{ databricks_token }}"
    state: absent
    name: "ansible-test-{{ resource_prefix }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: delete_idem

- name: Verify idempotent delete
  ansible.builtin.assert:
    that:
      - delete_idem is not changed
